import { ChordLyricsPair } from "../ChordLyricsPair";
import { Line } from "./Line";
export declare class LyricsLine extends Line {
    clone(): Line;
    get pairs(): ChordLyricsPair[];
    private _pairs;
    constructor(pairs?: ChordLyricsPair[]);
    addPair(pair: ChordLyricsPair): void;
}
