import { MusicLetter, MusicNote } from "../models";
export declare abstract class MusicNoteHelper {
    private static notes;
    private static accidentals;
    static semiTonesBetween(firstNote: MusicNote, secondNote: MusicNote): number;
    static letterDiff(firstLetter: MusicLetter, secondLetter: MusicLetter): number;
    static transpose(note: MusicNote, letterDiff: number, semiTones: number): MusicNote;
    private static positiveMod;
}
